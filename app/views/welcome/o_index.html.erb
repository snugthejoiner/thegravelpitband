<div class="jumbotron">
    <h1>The Gravel Pit Universe</h1>
      <% unless Show.upcoming.empty? %>
      <h2>Upcoming</h2>
      <% Show.upcoming.each do |show| %>
          <%= show.date.strftime('%A, %B %e, %Y') %>
          <%= show.descriptive %>
          <% if show.performances? %>
          <small><%= show.bill %></small>
          <% end %>
          <% end %>
      <% end %>
      <% unless Show.today_in_pit.empty? %>
        <h2>Today in Pit History</h2>
        <% Show.today_in_pit.each do |show| %>
          <%= show.date.strftime('%A, %B %e, %Y') %>
          <%= show.descriptive %>
          <% if show.performances? %>
          <small><%= show.bill %></small>
        <% end %><br>
        <% end %>
      <% end %>
</div>
<div class="row">
  <div class="col-md-4">
    <h2><%= link_to "Songs", songs_path %></h2>
    <p>Read the lyrics. <%= link_to "Sign Up", new_user_registration_path %> to become a Citizen and you can rate and comment on songs.</p>
  </div>
  <div class="col-md-4">
    <h2><%= link_to "Catalog", releases_path %></h2>
    <p>Ponder the recorded output. <%= link_to "Sign Up", new_user_registration_path %> to become a Citizen and you can rate and comment on releases.</p>  </div>
  <div class="col-md-4">
    <h2><%= link_to "Shows", shows_path %></h2>
    <p>The history of performances. <%= link_to "Sign Up", new_user_registration_path %> to become a Citizen and you can rate and comment on releases.</p>
  </div>
</div>
<div class="row">
  <div class="col-md-4">
    <h2><%= link_to "Sign Up", new_user_registration_path %></h2>
    <p>Become a Citizen of The Gravel Pit Universe. Rate and comment on music. Membership is free, you only need to provide an email address and a name.</p>
  </div>
  <div class="col-md-8">
    <h2><%= link_to "Find Your Place In The Universe", new_user_registration_path %></h2>
    <p>Go the extra mile, provide your birthdate and see your history intertwined with the history of the band.</p>  
  </div>
</div>