
<div class="row">
  <div class="col-md-12">
    <div class="listing">
      <%= link_to show.date.strftime('%A, %B %e, %Y'), show %>
        <%= show.description %>
        <% if show.performances? %>
          <small class="listing">with <%= show.bill %></small>
        <% end %>
    </div>
    </div>
</div>