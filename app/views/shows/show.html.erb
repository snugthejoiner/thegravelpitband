
<div class="row">
  <div class="col-md-12">
    <h2><%= render partial: 'show', locals: {show: @show} %></h2>
  </div>
</div>
<hr>

<div class="row">
  <div class="col-md-12">
    <% if user_signed_in? && current_user.admin? %>
      <%= link_to '| EDIT', edit_show_path(@show) %>
    <% end %>
    <%= link_to 'Back To Shows', shows_path %>
  </div>
</div>