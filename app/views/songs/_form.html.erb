<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this song from being saved:</h2>
      <ul>
      <% @song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <p><%= f.text_area :lyrics, class: 'form-control' %></p>
    <%= f.select(:release_id, options_for_select([["Serpent",1],["Extrackaganza",10],["Mass Ave",2],["Silver",3],["Manifesto",4],["Crash Land",5],["Favorite",6],["No One", 7]],@song.release_id)) %>
    <%= f.submit value="Update", class: 'btn btn-success' %>
  </div>
<% end %>
