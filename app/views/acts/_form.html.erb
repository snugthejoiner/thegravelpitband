<%= form_for(@act) do |f| %>
  <% if @act.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@act.errors.count, "error") %> prohibited this act from being saved:</h2>
      <ul>
      <% @act.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <p><%= f.text_field :name, autofocus: true, class: 'form-control' %></p>
    <%= f.submit value="Save", class: 'btn btn-success' %>
    <%= link_to 'Back', acts_path, class: 'btn btn-info'  %>
  </div>
<% end %>